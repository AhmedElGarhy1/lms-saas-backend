export enum UserProfileEvents {
  ACTIVATED = 'user_profile.activated',
  DEACTIVATED = 'user_profile.deactivated',
  DELETED = 'user_profile.deleted',
  RESTORED = 'user_profile.restored',
  CREATED = 'user_profile.created',
}
