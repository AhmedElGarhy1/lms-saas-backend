export enum GroupEvents {
  CREATE = 'group.create',
  CREATED = 'group.created',
  UPDATED = 'group.updated',
  SCHEDULE_ITEMS_UPDATED = 'group.schedule_items.updated',
  DELETED = 'group.deleted',
  RESTORED = 'group.restored',
  EXPORTED = 'group.exported',
}
